(1) we are using python opencv and tkinter libraries to build this project.
(2) Image Editing with OpenCV:
	OpenCV library is a backend image processing library using which we perform lot of image processing tasks such as cropping, drawing over images, adding text, applying filters and orientation.
(3) Tkinter GUI:
	Tkinter GUI is a python library using which users can edit the image easily without worrying about the implementation providing an abstract	view with the help of Tkinter GUI.
(4) fast response and easy to implement using python.